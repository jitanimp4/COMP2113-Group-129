#include "word_pyramid.h"

using namespace std;

/**
 * Program entry point; starts the Word Pyramids game.
 * Input parameters:
 * - argc: count of command line arguments (unused).
 * - argv: argument list (unused).
 * Return value:
 * - 0 on normal termination.
 */
int main(int argc, char* argv[]) {
  (void)argc;
  (void)argv;
  runWordPyramidGame();
  return 0;
}
